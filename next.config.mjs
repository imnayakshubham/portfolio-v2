/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', "bit.ly", "plus.unsplash.com", "media.istockphoto.com", "avatars.githubusercontent.com"],
  },
};

export default nextConfig;
